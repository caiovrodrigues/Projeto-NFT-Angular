import { LimitenamePipe } from './limitename.pipe';

describe('LimitenamePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitenamePipe();
    expect(pipe).toBeTruthy();
  });
});
